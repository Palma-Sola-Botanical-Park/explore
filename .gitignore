# ───────────────────────────────
# macOS Finder junk
# ───────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride

# ───────────────────────────────
# Python
# ───────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ───────────────────────────────
# PSBP scratch / cache files
# (enrichment outputs, API caches, temp)
# ───────────────────────────────
photo_credits.enriched.json
.obs_cache.json
*.tmp
*.bak
*.bak-*
*.bak.*
data/sources/.placements_backups/

# ───────────────────────────────
# Booby-trap: the cwd-mistake folder.
# Real photos live in their own folder and stay tracked.
# If a misfire ever dumps images HERE again, git ignores them
# instead of committing a thousand files.
# ───────────────────────────────
data/sources/photos/

# ───────────────────────────────
# Photos OF the Bright Futures students (minors) — Randy, 2026-09-25:
# "ill keep on office computer." Local-only on purpose: the repo is
# public, this isn't. The kiosk still serves these fine since it reads
# straight off the local clone; they just never reach GitHub.
# ───────────────────────────────
images/brightfutures-local/
repo_scan.sh
repo_scan.txt
.DS_Store
